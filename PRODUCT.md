# Product

## Register

product

## Users

本地单用户环境中的业务分析人员和数据建模人员。他们在桌面 Web 工作区中维护 SelectDB 业务本体、发起自然语言分析，并检查每轮分析的语义绑定、查询结果与证据链。

## Product Purpose

InsightFlow 将业务问题通过已发布的 Ontology 转换为受约束的只读 SelectDB 查询，优先呈现可信结果，并让语义口径、关系路径、SQL 和执行状态始终可追溯。成功意味着用户能连续问数、增量维护本体，并清楚判断结果为何可信。

## Brand Personality

可信、克制、清晰。界面语气专业直接，强调确定性、可检查性和低认知负担。

## Anti-references

不采用装饰性强、状态含糊或信息密度失控的通用 SaaS 仪表盘风格；不以颜色作为唯一状态表达；不把数据连接信息带入问数页面；不让视觉效果抢过结果、约束和证据。

## Design Principles

- 结果优先，核心分析产物先于过程细节。
- 约束可见，让语义口径、查询边界和当前状态易于检查。
- 状态无歧义，同时使用文字与图标表达关键状态。
- 逐层披露，先提供决策所需信息，再允许用户深入证据链。
- 对话即工作区，多轮上下文连续，但每轮证据独立保留。

## Accessibility & Inclusion

正文与控件达到 WCAG AA。状态不只依赖颜色，交互具备键盘焦点反馈；动效支持 `prefers-reduced-motion`，中文界面优先保证清晰字号和可读行高。
